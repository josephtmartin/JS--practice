const calculateAgeInDogYears = (age) => {
  const ageInDogYears = age / 7;

  return ageInDogYears;
};

const dogAge = calculateAgeInDogYears(22);
console.log(dogAge);

const dogBreed = (string) => {
  const 
};
